import type { Metadata } from "next";
import { ThemeC } from "@/themes/theme-c";

export const metadata: Metadata = {
  title: "Apurba Dutta — Concept Spec",
};

export default function ConceptPage() {
  return <ThemeC />;
}